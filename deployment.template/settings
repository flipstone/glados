REPO=quay.io/flipstone/glados
CURRENT_VERSION=20150209-073353
READLINK=$(which greadlink || echo "readlink")

SUDO="sudo"

if [ "$(uname)" = "Darwin" ]; then
  SUDO=""
fi;

DEPLOY_ROOT=$($READLINK -f $DEPLOY_ROOT)
TASKS=$DEPLOY_ROOT/tasks
HOST_PORT=26320
CONTAINER_PORT=8000
CONTAINER_NAME=glados

